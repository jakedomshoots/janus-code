# Janus Code 0.0.1-rc.8

## macOS Direct Downloads

These macOS downloads are unsigned direct-download builds. On first launch, macOS may block the app from opening normally. Use right-click -> Open from Finder, then confirm Open when macOS asks.

Verify downloaded files with the SHA-256 checksums in `SHA256SUMS.txt` before opening the app.
